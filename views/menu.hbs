<ul class="coffee__list">
  {{#each items}}
  <li>
    <header class="coffee__name">{{name}}</header>
    <img src="{{image}}" alt="{{name}}">
    <a href="/buy/{{name}}" class="link action">
      В корзину ({{price}} ₽)
	
		
    </a>
	Обьём: {{volume}}<br>
	Вес: {{weight}}н
  </li>
  {{/each}}
</ul>